# GeminiCaller

Python background worker.

For local development, set `GEMINI_MODE=fake`.

The worker should eventually load cameras, call Gemini, validate structured output, write operator actions, and track Gemini usage/cost.
