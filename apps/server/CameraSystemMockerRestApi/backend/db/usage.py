# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
# source: usage.sql
import dataclasses
import decimal
from typing import Any, Optional

import sqlalchemy
import sqlalchemy.ext.asyncio

from db import models


CREATE_USAGE_EVENT = """-- name: create_usage_event \\:one
INSERT INTO usage_events (id, operation_id, camera_id, camera_group_id, event_type, estimated_cost, token_count)
VALUES (COALESCE(NULLIF(:p1, ''), gen_random_uuid()\\:\\:text), :p2, :p3, :p4, :p5, COALESCE(:p6, 0), COALESCE(:p7, 0))
RETURNING id, operation_id, camera_id, camera_group_id, event_type, estimated_cost, token_count, created_at
"""


@dataclasses.dataclass()
class CreateUsageEventParams:
    column_1: Optional[Any]
    operation_id: Optional[str]
    camera_id: Optional[str]
    camera_group_id: Optional[str]
    event_type: str
    column_6: Optional[Any]
    column_7: Optional[Any]


GET_USAGE_SUMMARY = """-- name: get_usage_summary \\:one
SELECT
  COUNT(*) FILTER (WHERE event_type = 'scan' AND created_at >= date_trunc('day', now()))\\:\\:int AS scans_today,
  COUNT(*) FILTER (WHERE event_type = 'scan' AND created_at >= date_trunc('month', now()))\\:\\:int AS scans_this_month,
  COALESCE(SUM(estimated_cost) FILTER (WHERE created_at >= date_trunc('day', now())), 0)\\:\\:numeric AS estimated_cost_today,
  COALESCE(SUM(estimated_cost) FILTER (WHERE created_at >= date_trunc('month', now())), 0)\\:\\:numeric AS estimated_cost_this_month,
  now() AS last_updated_at
FROM usage_events
"""


@dataclasses.dataclass()
class GetUsageSummaryRow:
    scans_today: int
    scans_this_month: int
    estimated_cost_today: decimal.Decimal
    estimated_cost_this_month: decimal.Decimal
    last_updated_at: Any


GET_USAGE_SUMMARY_FOR_CAMERA = """-- name: get_usage_summary_for_camera \\:one
SELECT
  COUNT(*) FILTER (WHERE event_type = 'scan' AND created_at >= date_trunc('day', now()))\\:\\:int AS scans_today,
  COUNT(*) FILTER (WHERE event_type = 'scan' AND created_at >= date_trunc('month', now()))\\:\\:int AS scans_this_month,
  COALESCE(SUM(estimated_cost) FILTER (WHERE created_at >= date_trunc('day', now())), 0)\\:\\:numeric AS estimated_cost_today,
  COALESCE(SUM(estimated_cost) FILTER (WHERE created_at >= date_trunc('month', now())), 0)\\:\\:numeric AS estimated_cost_this_month,
  now() AS last_updated_at
FROM usage_events
WHERE camera_id = :p1
"""


@dataclasses.dataclass()
class GetUsageSummaryForCameraRow:
    scans_today: int
    scans_this_month: int
    estimated_cost_today: decimal.Decimal
    estimated_cost_this_month: decimal.Decimal
    last_updated_at: Any


GET_USAGE_SUMMARY_FOR_CAMERA_GROUP = """-- name: get_usage_summary_for_camera_group \\:one
SELECT
  COUNT(*) FILTER (WHERE event_type = 'scan' AND created_at >= date_trunc('day', now()))\\:\\:int AS scans_today,
  COUNT(*) FILTER (WHERE event_type = 'scan' AND created_at >= date_trunc('month', now()))\\:\\:int AS scans_this_month,
  COALESCE(SUM(estimated_cost) FILTER (WHERE created_at >= date_trunc('day', now())), 0)\\:\\:numeric AS estimated_cost_today,
  COALESCE(SUM(estimated_cost) FILTER (WHERE created_at >= date_trunc('month', now())), 0)\\:\\:numeric AS estimated_cost_this_month,
  now() AS last_updated_at
FROM usage_events
WHERE camera_group_id = :p1
"""


@dataclasses.dataclass()
class GetUsageSummaryForCameraGroupRow:
    scans_today: int
    scans_this_month: int
    estimated_cost_today: decimal.Decimal
    estimated_cost_this_month: decimal.Decimal
    last_updated_at: Any


class Querier:
    def __init__(self, conn: sqlalchemy.engine.Connection):
        self._conn = conn

    def create_usage_event(self, arg: CreateUsageEventParams) -> Optional[models.UsageEvent]:
        row = self._conn.execute(sqlalchemy.text(CREATE_USAGE_EVENT), {
            "p1": arg.column_1,
            "p2": arg.operation_id,
            "p3": arg.camera_id,
            "p4": arg.camera_group_id,
            "p5": arg.event_type,
            "p6": arg.column_6,
            "p7": arg.column_7,
        }).first()
        if row is None:
            return None
        return models.UsageEvent(
            id=row[0],
            operation_id=row[1],
            camera_id=row[2],
            camera_group_id=row[3],
            event_type=row[4],
            estimated_cost=row[5],
            token_count=row[6],
            created_at=row[7],
        )

    def get_usage_summary(self) -> Optional[GetUsageSummaryRow]:
        row = self._conn.execute(sqlalchemy.text(GET_USAGE_SUMMARY)).first()
        if row is None:
            return None
        return GetUsageSummaryRow(
            scans_today=row[0],
            scans_this_month=row[1],
            estimated_cost_today=row[2],
            estimated_cost_this_month=row[3],
            last_updated_at=row[4],
        )

    def get_usage_summary_for_camera(self, *, camera_id: Optional[str]) -> Optional[GetUsageSummaryForCameraRow]:
        row = self._conn.execute(sqlalchemy.text(GET_USAGE_SUMMARY_FOR_CAMERA), {"p1": camera_id}).first()
        if row is None:
            return None
        return GetUsageSummaryForCameraRow(
            scans_today=row[0],
            scans_this_month=row[1],
            estimated_cost_today=row[2],
            estimated_cost_this_month=row[3],
            last_updated_at=row[4],
        )

    def get_usage_summary_for_camera_group(self, *, camera_group_id: Optional[str]) -> Optional[GetUsageSummaryForCameraGroupRow]:
        row = self._conn.execute(sqlalchemy.text(GET_USAGE_SUMMARY_FOR_CAMERA_GROUP), {"p1": camera_group_id}).first()
        if row is None:
            return None
        return GetUsageSummaryForCameraGroupRow(
            scans_today=row[0],
            scans_this_month=row[1],
            estimated_cost_today=row[2],
            estimated_cost_this_month=row[3],
            last_updated_at=row[4],
        )


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def create_usage_event(self, arg: CreateUsageEventParams) -> Optional[models.UsageEvent]:
        row = (await self._conn.execute(sqlalchemy.text(CREATE_USAGE_EVENT), {
            "p1": arg.column_1,
            "p2": arg.operation_id,
            "p3": arg.camera_id,
            "p4": arg.camera_group_id,
            "p5": arg.event_type,
            "p6": arg.column_6,
            "p7": arg.column_7,
        })).first()
        if row is None:
            return None
        return models.UsageEvent(
            id=row[0],
            operation_id=row[1],
            camera_id=row[2],
            camera_group_id=row[3],
            event_type=row[4],
            estimated_cost=row[5],
            token_count=row[6],
            created_at=row[7],
        )

    async def get_usage_summary(self) -> Optional[GetUsageSummaryRow]:
        row = (await self._conn.execute(sqlalchemy.text(GET_USAGE_SUMMARY))).first()
        if row is None:
            return None
        return GetUsageSummaryRow(
            scans_today=row[0],
            scans_this_month=row[1],
            estimated_cost_today=row[2],
            estimated_cost_this_month=row[3],
            last_updated_at=row[4],
        )

    async def get_usage_summary_for_camera(self, *, camera_id: Optional[str]) -> Optional[GetUsageSummaryForCameraRow]:
        row = (await self._conn.execute(sqlalchemy.text(GET_USAGE_SUMMARY_FOR_CAMERA), {"p1": camera_id})).first()
        if row is None:
            return None
        return GetUsageSummaryForCameraRow(
            scans_today=row[0],
            scans_this_month=row[1],
            estimated_cost_today=row[2],
            estimated_cost_this_month=row[3],
            last_updated_at=row[4],
        )

    async def get_usage_summary_for_camera_group(self, *, camera_group_id: Optional[str]) -> Optional[GetUsageSummaryForCameraGroupRow]:
        row = (await self._conn.execute(sqlalchemy.text(GET_USAGE_SUMMARY_FOR_CAMERA_GROUP), {"p1": camera_group_id})).first()
        if row is None:
            return None
        return GetUsageSummaryForCameraGroupRow(
            scans_today=row[0],
            scans_this_month=row[1],
            estimated_cost_today=row[2],
            estimated_cost_this_month=row[3],
            last_updated_at=row[4],
        )
