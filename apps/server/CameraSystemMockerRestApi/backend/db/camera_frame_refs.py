# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
# source: camera_frame_refs.sql
import dataclasses
import datetime
from typing import Any, AsyncIterator, Iterator, Optional

import sqlalchemy
import sqlalchemy.ext.asyncio

from db import models


ATTACH_FRAME_REF_TO_OPERATION = """-- name: attach_frame_ref_to_operation \\:exec
INSERT INTO operation_frame_refs (
  operation_id,
  frame_ref_id,
  purpose
)
VALUES (
  :p1,
  :p2,
  COALESCE(:p3, 'input')
)
ON CONFLICT (operation_id, frame_ref_id)
DO NOTHING
"""


CREATE_CAMERA_FRAME_REF = """-- name: create_camera_frame_ref \\:one
INSERT INTO camera_frame_refs (
  id,
  camera_id,
  frame_id,
  snapshot_id,
  frame_url,
  sequence_number,
  captured_at,
  mime_type,
  width,
  height,
  expires_at
)
VALUES (
  COALESCE(NULLIF(:p1, ''), gen_random_uuid()\\:\\:text),
  :p2,
  :p3,
  :p4,
  :p5,
  :p6,
  :p7,
  COALESCE(:p8, 'image/jpeg'),
  :p9,
  :p10,
  :p11
)
ON CONFLICT (camera_id, frame_id)
DO UPDATE SET
  snapshot_id = EXCLUDED.snapshot_id,
  frame_url = EXCLUDED.frame_url,
  sequence_number = EXCLUDED.sequence_number,
  captured_at = EXCLUDED.captured_at,
  mime_type = EXCLUDED.mime_type,
  width = EXCLUDED.width,
  height = EXCLUDED.height,
  expires_at = EXCLUDED.expires_at
RETURNING id, camera_id, frame_id, snapshot_id, frame_url, sequence_number, captured_at, mime_type, width, height, expires_at, created_at, updated_at
"""


@dataclasses.dataclass()
class CreateCameraFrameRefParams:
    id: Optional[Any]
    camera_id: str
    frame_id: str
    snapshot_id: Optional[str]
    frame_url: str
    sequence_number: Optional[int]
    captured_at: datetime.datetime
    mime_type: Optional[Any]
    width: Optional[int]
    height: Optional[int]
    expires_at: Optional[datetime.datetime]


DETACH_FRAME_REF_FROM_OPERATION = """-- name: detach_frame_ref_from_operation \\:exec
DELETE FROM operation_frame_refs
WHERE operation_id = :p1
  AND frame_ref_id = :p2
"""


GET_CAMERA_FRAME_REF = """-- name: get_camera_frame_ref \\:one
SELECT id, camera_id, frame_id, snapshot_id, frame_url, sequence_number, captured_at, mime_type, width, height, expires_at, created_at, updated_at
FROM camera_frame_refs
WHERE camera_id = :p1
  AND frame_id = :p2
"""


GET_CAMERA_FRAME_REF_BY_ID = """-- name: get_camera_frame_ref_by_id \\:one
SELECT id, camera_id, frame_id, snapshot_id, frame_url, sequence_number, captured_at, mime_type, width, height, expires_at, created_at, updated_at
FROM camera_frame_refs
WHERE id = :p1
"""


GET_LATEST_CAMERA_FRAME_REF_FOR_CAMERA = """-- name: get_latest_camera_frame_ref_for_camera \\:one
SELECT id, camera_id, frame_id, snapshot_id, frame_url, sequence_number, captured_at, mime_type, width, height, expires_at, created_at, updated_at
FROM camera_frame_refs
WHERE camera_id = :p1
ORDER BY captured_at DESC
LIMIT 1
"""


LIST_CAMERA_FRAME_REFS_FOR_CAMERA = """-- name: list_camera_frame_refs_for_camera \\:many
SELECT id, camera_id, frame_id, snapshot_id, frame_url, sequence_number, captured_at, mime_type, width, height, expires_at, created_at, updated_at
FROM camera_frame_refs
WHERE camera_id = :p1
ORDER BY captured_at DESC
LIMIT :p3 OFFSET :p2
"""


LIST_FRAME_REFS_FOR_OPERATION = """-- name: list_frame_refs_for_operation \\:many
SELECT cfr.id, cfr.camera_id, cfr.frame_id, cfr.snapshot_id, cfr.frame_url, cfr.sequence_number, cfr.captured_at, cfr.mime_type, cfr.width, cfr.height, cfr.expires_at, cfr.created_at, cfr.updated_at
FROM camera_frame_refs cfr
JOIN operation_frame_refs ofr
  ON ofr.frame_ref_id = cfr.id
WHERE ofr.operation_id = :p1
ORDER BY cfr.captured_at ASC
"""


class Querier:
    def __init__(self, conn: sqlalchemy.engine.Connection):
        self._conn = conn

    def attach_frame_ref_to_operation(self, *, operation_id: str, frame_ref_id: str, purpose: Optional[Any]) -> None:
        self._conn.execute(sqlalchemy.text(ATTACH_FRAME_REF_TO_OPERATION), {"p1": operation_id, "p2": frame_ref_id, "p3": purpose})

    def create_camera_frame_ref(self, arg: CreateCameraFrameRefParams) -> Optional[models.CameraFrameRef]:
        row = self._conn.execute(sqlalchemy.text(CREATE_CAMERA_FRAME_REF), {
            "p1": arg.id,
            "p2": arg.camera_id,
            "p3": arg.frame_id,
            "p4": arg.snapshot_id,
            "p5": arg.frame_url,
            "p6": arg.sequence_number,
            "p7": arg.captured_at,
            "p8": arg.mime_type,
            "p9": arg.width,
            "p10": arg.height,
            "p11": arg.expires_at,
        }).first()
        if row is None:
            return None
        return models.CameraFrameRef(
            id=row[0],
            camera_id=row[1],
            frame_id=row[2],
            snapshot_id=row[3],
            frame_url=row[4],
            sequence_number=row[5],
            captured_at=row[6],
            mime_type=row[7],
            width=row[8],
            height=row[9],
            expires_at=row[10],
            created_at=row[11],
            updated_at=row[12],
        )

    def detach_frame_ref_from_operation(self, *, operation_id: str, frame_ref_id: str) -> None:
        self._conn.execute(sqlalchemy.text(DETACH_FRAME_REF_FROM_OPERATION), {"p1": operation_id, "p2": frame_ref_id})

    def get_camera_frame_ref(self, *, camera_id: str, frame_id: str) -> Optional[models.CameraFrameRef]:
        row = self._conn.execute(sqlalchemy.text(GET_CAMERA_FRAME_REF), {"p1": camera_id, "p2": frame_id}).first()
        if row is None:
            return None
        return models.CameraFrameRef(
            id=row[0],
            camera_id=row[1],
            frame_id=row[2],
            snapshot_id=row[3],
            frame_url=row[4],
            sequence_number=row[5],
            captured_at=row[6],
            mime_type=row[7],
            width=row[8],
            height=row[9],
            expires_at=row[10],
            created_at=row[11],
            updated_at=row[12],
        )

    def get_camera_frame_ref_by_id(self, *, id: str) -> Optional[models.CameraFrameRef]:
        row = self._conn.execute(sqlalchemy.text(GET_CAMERA_FRAME_REF_BY_ID), {"p1": id}).first()
        if row is None:
            return None
        return models.CameraFrameRef(
            id=row[0],
            camera_id=row[1],
            frame_id=row[2],
            snapshot_id=row[3],
            frame_url=row[4],
            sequence_number=row[5],
            captured_at=row[6],
            mime_type=row[7],
            width=row[8],
            height=row[9],
            expires_at=row[10],
            created_at=row[11],
            updated_at=row[12],
        )

    def get_latest_camera_frame_ref_for_camera(self, *, camera_id: str) -> Optional[models.CameraFrameRef]:
        row = self._conn.execute(sqlalchemy.text(GET_LATEST_CAMERA_FRAME_REF_FOR_CAMERA), {"p1": camera_id}).first()
        if row is None:
            return None
        return models.CameraFrameRef(
            id=row[0],
            camera_id=row[1],
            frame_id=row[2],
            snapshot_id=row[3],
            frame_url=row[4],
            sequence_number=row[5],
            captured_at=row[6],
            mime_type=row[7],
            width=row[8],
            height=row[9],
            expires_at=row[10],
            created_at=row[11],
            updated_at=row[12],
        )

    def list_camera_frame_refs_for_camera(self, *, camera_id: str, offset_count: int, limit_count: int) -> Iterator[models.CameraFrameRef]:
        result = self._conn.execute(sqlalchemy.text(LIST_CAMERA_FRAME_REFS_FOR_CAMERA), {"p1": camera_id, "p2": offset_count, "p3": limit_count})
        for row in result:
            yield models.CameraFrameRef(
                id=row[0],
                camera_id=row[1],
                frame_id=row[2],
                snapshot_id=row[3],
                frame_url=row[4],
                sequence_number=row[5],
                captured_at=row[6],
                mime_type=row[7],
                width=row[8],
                height=row[9],
                expires_at=row[10],
                created_at=row[11],
                updated_at=row[12],
            )

    def list_frame_refs_for_operation(self, *, operation_id: str) -> Iterator[models.CameraFrameRef]:
        result = self._conn.execute(sqlalchemy.text(LIST_FRAME_REFS_FOR_OPERATION), {"p1": operation_id})
        for row in result:
            yield models.CameraFrameRef(
                id=row[0],
                camera_id=row[1],
                frame_id=row[2],
                snapshot_id=row[3],
                frame_url=row[4],
                sequence_number=row[5],
                captured_at=row[6],
                mime_type=row[7],
                width=row[8],
                height=row[9],
                expires_at=row[10],
                created_at=row[11],
                updated_at=row[12],
            )


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def attach_frame_ref_to_operation(self, *, operation_id: str, frame_ref_id: str, purpose: Optional[Any]) -> None:
        await self._conn.execute(sqlalchemy.text(ATTACH_FRAME_REF_TO_OPERATION), {"p1": operation_id, "p2": frame_ref_id, "p3": purpose})

    async def create_camera_frame_ref(self, arg: CreateCameraFrameRefParams) -> Optional[models.CameraFrameRef]:
        row = (await self._conn.execute(sqlalchemy.text(CREATE_CAMERA_FRAME_REF), {
            "p1": arg.id,
            "p2": arg.camera_id,
            "p3": arg.frame_id,
            "p4": arg.snapshot_id,
            "p5": arg.frame_url,
            "p6": arg.sequence_number,
            "p7": arg.captured_at,
            "p8": arg.mime_type,
            "p9": arg.width,
            "p10": arg.height,
            "p11": arg.expires_at,
        })).first()
        if row is None:
            return None
        return models.CameraFrameRef(
            id=row[0],
            camera_id=row[1],
            frame_id=row[2],
            snapshot_id=row[3],
            frame_url=row[4],
            sequence_number=row[5],
            captured_at=row[6],
            mime_type=row[7],
            width=row[8],
            height=row[9],
            expires_at=row[10],
            created_at=row[11],
            updated_at=row[12],
        )

    async def detach_frame_ref_from_operation(self, *, operation_id: str, frame_ref_id: str) -> None:
        await self._conn.execute(sqlalchemy.text(DETACH_FRAME_REF_FROM_OPERATION), {"p1": operation_id, "p2": frame_ref_id})

    async def get_camera_frame_ref(self, *, camera_id: str, frame_id: str) -> Optional[models.CameraFrameRef]:
        row = (await self._conn.execute(sqlalchemy.text(GET_CAMERA_FRAME_REF), {"p1": camera_id, "p2": frame_id})).first()
        if row is None:
            return None
        return models.CameraFrameRef(
            id=row[0],
            camera_id=row[1],
            frame_id=row[2],
            snapshot_id=row[3],
            frame_url=row[4],
            sequence_number=row[5],
            captured_at=row[6],
            mime_type=row[7],
            width=row[8],
            height=row[9],
            expires_at=row[10],
            created_at=row[11],
            updated_at=row[12],
        )

    async def get_camera_frame_ref_by_id(self, *, id: str) -> Optional[models.CameraFrameRef]:
        row = (await self._conn.execute(sqlalchemy.text(GET_CAMERA_FRAME_REF_BY_ID), {"p1": id})).first()
        if row is None:
            return None
        return models.CameraFrameRef(
            id=row[0],
            camera_id=row[1],
            frame_id=row[2],
            snapshot_id=row[3],
            frame_url=row[4],
            sequence_number=row[5],
            captured_at=row[6],
            mime_type=row[7],
            width=row[8],
            height=row[9],
            expires_at=row[10],
            created_at=row[11],
            updated_at=row[12],
        )

    async def get_latest_camera_frame_ref_for_camera(self, *, camera_id: str) -> Optional[models.CameraFrameRef]:
        row = (await self._conn.execute(sqlalchemy.text(GET_LATEST_CAMERA_FRAME_REF_FOR_CAMERA), {"p1": camera_id})).first()
        if row is None:
            return None
        return models.CameraFrameRef(
            id=row[0],
            camera_id=row[1],
            frame_id=row[2],
            snapshot_id=row[3],
            frame_url=row[4],
            sequence_number=row[5],
            captured_at=row[6],
            mime_type=row[7],
            width=row[8],
            height=row[9],
            expires_at=row[10],
            created_at=row[11],
            updated_at=row[12],
        )

    async def list_camera_frame_refs_for_camera(self, *, camera_id: str, offset_count: int, limit_count: int) -> AsyncIterator[models.CameraFrameRef]:
        result = await self._conn.stream(sqlalchemy.text(LIST_CAMERA_FRAME_REFS_FOR_CAMERA), {"p1": camera_id, "p2": offset_count, "p3": limit_count})
        async for row in result:
            yield models.CameraFrameRef(
                id=row[0],
                camera_id=row[1],
                frame_id=row[2],
                snapshot_id=row[3],
                frame_url=row[4],
                sequence_number=row[5],
                captured_at=row[6],
                mime_type=row[7],
                width=row[8],
                height=row[9],
                expires_at=row[10],
                created_at=row[11],
                updated_at=row[12],
            )

    async def list_frame_refs_for_operation(self, *, operation_id: str) -> AsyncIterator[models.CameraFrameRef]:
        result = await self._conn.stream(sqlalchemy.text(LIST_FRAME_REFS_FOR_OPERATION), {"p1": operation_id})
        async for row in result:
            yield models.CameraFrameRef(
                id=row[0],
                camera_id=row[1],
                frame_id=row[2],
                snapshot_id=row[3],
                frame_url=row[4],
                sequence_number=row[5],
                captured_at=row[6],
                mime_type=row[7],
                width=row[8],
                height=row[9],
                expires_at=row[10],
                created_at=row[11],
                updated_at=row[12],
            )
