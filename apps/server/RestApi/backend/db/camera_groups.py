# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
# source: camera_groups.sql
from typing import Any, AsyncIterator, Iterator, List, Optional

import sqlalchemy
import sqlalchemy.ext.asyncio

from db import models


CREATE_CAMERA_GROUP = """-- name: create_camera_group \\:one
INSERT INTO camera_groups (id, name, description, camera_ids)
VALUES (COALESCE(NULLIF(:p1, ''), gen_random_uuid()\\:\\:text), :p2, :p3, COALESCE(:p4, '{}'\\:\\:text[]))
RETURNING id, name, description, camera_ids, created_at, updated_at
"""


DELETE_CAMERA_GROUP = """-- name: delete_camera_group \\:exec
DELETE FROM camera_groups
WHERE id = :p1
"""


GET_CAMERA_GROUP = """-- name: get_camera_group \\:one
SELECT id, name, description, camera_ids, created_at, updated_at
FROM camera_groups
WHERE id = :p1
"""


LIST_CAMERA_GROUPS = """-- name: list_camera_groups \\:many
SELECT id, name, description, camera_ids, created_at, updated_at
FROM camera_groups
ORDER BY name ASC
"""


REPLACE_CAMERA_GROUP_CAMERAS = """-- name: replace_camera_group_cameras \\:one
UPDATE camera_groups
SET camera_ids = COALESCE(:p2, '{}'\\:\\:text[])
WHERE id = :p1
RETURNING id, name, description, camera_ids, created_at, updated_at
"""


UPDATE_CAMERA_GROUP = """-- name: update_camera_group \\:one
UPDATE camera_groups
SET name = COALESCE(:p2, name), description = COALESCE(:p3, description)
WHERE id = :p1
RETURNING id, name, description, camera_ids, created_at, updated_at
"""


class Querier:
    def __init__(self, conn: sqlalchemy.engine.Connection):
        self._conn = conn

    def create_camera_group(self, *, dollar_1: Optional[Any], name: str, description: Optional[str], dollar_4: Optional[Any]) -> Optional[models.CameraGroup]:
        row = self._conn.execute(sqlalchemy.text(CREATE_CAMERA_GROUP), {
            "p1": dollar_1,
            "p2": name,
            "p3": description,
            "p4": dollar_4,
        }).first()
        if row is None:
            return None
        return models.CameraGroup(
            id=row[0],
            name=row[1],
            description=row[2],
            camera_ids=row[3],
            created_at=row[4],
            updated_at=row[5],
        )

    def delete_camera_group(self, *, id: str) -> None:
        self._conn.execute(sqlalchemy.text(DELETE_CAMERA_GROUP), {"p1": id})

    def get_camera_group(self, *, id: str) -> Optional[models.CameraGroup]:
        row = self._conn.execute(sqlalchemy.text(GET_CAMERA_GROUP), {"p1": id}).first()
        if row is None:
            return None
        return models.CameraGroup(
            id=row[0],
            name=row[1],
            description=row[2],
            camera_ids=row[3],
            created_at=row[4],
            updated_at=row[5],
        )

    def list_camera_groups(self) -> Iterator[models.CameraGroup]:
        result = self._conn.execute(sqlalchemy.text(LIST_CAMERA_GROUPS))
        for row in result:
            yield models.CameraGroup(
                id=row[0],
                name=row[1],
                description=row[2],
                camera_ids=row[3],
                created_at=row[4],
                updated_at=row[5],
            )

    def replace_camera_group_cameras(self, *, id: str, camera_ids: List[str]) -> Optional[models.CameraGroup]:
        row = self._conn.execute(sqlalchemy.text(REPLACE_CAMERA_GROUP_CAMERAS), {"p1": id, "p2": camera_ids}).first()
        if row is None:
            return None
        return models.CameraGroup(
            id=row[0],
            name=row[1],
            description=row[2],
            camera_ids=row[3],
            created_at=row[4],
            updated_at=row[5],
        )

    def update_camera_group(self, *, id: str, name: str, description: Optional[str]) -> Optional[models.CameraGroup]:
        row = self._conn.execute(sqlalchemy.text(UPDATE_CAMERA_GROUP), {"p1": id, "p2": name, "p3": description}).first()
        if row is None:
            return None
        return models.CameraGroup(
            id=row[0],
            name=row[1],
            description=row[2],
            camera_ids=row[3],
            created_at=row[4],
            updated_at=row[5],
        )


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def create_camera_group(self, *, dollar_1: Optional[Any], name: str, description: Optional[str], dollar_4: Optional[Any]) -> Optional[models.CameraGroup]:
        row = (await self._conn.execute(sqlalchemy.text(CREATE_CAMERA_GROUP), {
            "p1": dollar_1,
            "p2": name,
            "p3": description,
            "p4": dollar_4,
        })).first()
        if row is None:
            return None
        return models.CameraGroup(
            id=row[0],
            name=row[1],
            description=row[2],
            camera_ids=row[3],
            created_at=row[4],
            updated_at=row[5],
        )

    async def delete_camera_group(self, *, id: str) -> None:
        await self._conn.execute(sqlalchemy.text(DELETE_CAMERA_GROUP), {"p1": id})

    async def get_camera_group(self, *, id: str) -> Optional[models.CameraGroup]:
        row = (await self._conn.execute(sqlalchemy.text(GET_CAMERA_GROUP), {"p1": id})).first()
        if row is None:
            return None
        return models.CameraGroup(
            id=row[0],
            name=row[1],
            description=row[2],
            camera_ids=row[3],
            created_at=row[4],
            updated_at=row[5],
        )

    async def list_camera_groups(self) -> AsyncIterator[models.CameraGroup]:
        result = await self._conn.stream(sqlalchemy.text(LIST_CAMERA_GROUPS))
        async for row in result:
            yield models.CameraGroup(
                id=row[0],
                name=row[1],
                description=row[2],
                camera_ids=row[3],
                created_at=row[4],
                updated_at=row[5],
            )

    async def replace_camera_group_cameras(self, *, id: str, camera_ids: List[str]) -> Optional[models.CameraGroup]:
        row = (await self._conn.execute(sqlalchemy.text(REPLACE_CAMERA_GROUP_CAMERAS), {"p1": id, "p2": camera_ids})).first()
        if row is None:
            return None
        return models.CameraGroup(
            id=row[0],
            name=row[1],
            description=row[2],
            camera_ids=row[3],
            created_at=row[4],
            updated_at=row[5],
        )

    async def update_camera_group(self, *, id: str, name: str, description: Optional[str]) -> Optional[models.CameraGroup]:
        row = (await self._conn.execute(sqlalchemy.text(UPDATE_CAMERA_GROUP), {"p1": id, "p2": name, "p3": description})).first()
        if row is None:
            return None
        return models.CameraGroup(
            id=row[0],
            name=row[1],
            description=row[2],
            camera_ids=row[3],
            created_at=row[4],
            updated_at=row[5],
        )
