name: Deploy CamBot Prod

on:
  push:
    branches:
      - main

jobs:
  deploy-prod:
    uses: ./.github/workflows/deploy-core.yml
    with:
      environment: prod
      branch_environment: prod
    secrets: inherit
